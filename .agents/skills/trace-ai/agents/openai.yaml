interface:
  display_name: "Trace Ai"
  short_description: "Repo-specific patterns and workflows for trace-ai"
  default_prompt: "Use the trace-ai repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true